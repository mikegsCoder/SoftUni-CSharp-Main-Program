<div class="container">
    Your request is not valid. Errors:

    <ul>
        @foreach (var error in @Model.AllErrorsViewModels)
        {
            <li>@error.ErrorMessage</li>
        }
    </ul>
</div>
